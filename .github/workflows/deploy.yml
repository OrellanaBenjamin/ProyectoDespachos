name: CI/CD Deploy

on:
  push:
    branches:
      - deploy

jobs:

  build-and-push:
    runs-on: ubuntu-latest

    steps:

      - name: Clonar repositorio
        uses: actions/checkout@v4

      - name: Login Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
 
 
      - name: Build frontend
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME }}/proyectodespachos/front_despacho:latest ./frontend

      - name: Push frontend
        run: |
          docker push ${{ secrets.DOCKER_USERNAME }}/proyectodespachos/front_despacho:latest

      - name: Build backend
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME }}/proyectodespachos/back-Despachos_SpringBoot:latest ./backend

      - name: Push backend
        run: |
          docker push ${{ secrets.DOCKER_USERNAME }}/proyectodespachos/back-Despachos_SpringBoot:latest

      - name: Build backendend
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME }}/proyectodespachos/back-Ventas_SpringBoot:latest ./backend

      - name: Push backendend
        run: |
          docker push ${{ secrets.DOCKER_USERNAME }}/proyectodespachos/back-Ventas_SpringBoot:latest